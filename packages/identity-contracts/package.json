{
  "name": "@identity-service/contracts",
  "version": "1.0.0",
  "description": "Shared contracts, DTOs, and client for Identity Service — independently published package",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "pact"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint --ext .ts src --no-error-on-unmatched-pattern",
    "test": "vitest run"
  },
  "peerDependencies": {
    "zod": "^3.23.0"
  },
  "dependencies": {
    "@pact-foundation/pact": "^12.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0",
    "zod": "^3.23.0"
  },
  "sideEffects": false
}
